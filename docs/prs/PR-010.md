[PR-010: افزودن کاتالوگ فروش (محصول/واریانت/مدیا/مجموعه)]

1) هدف این PR (چرا)
   - ایجاد کاتالوگ محصولات برای Site OS با اتصال به موجودی/کالای حسابداری و نرخ ارز.

2) چه چیزهایی تغییر کرد (What changed)
   - پکیج `haida/commerce-catalog` شامل مدل‌ها، مهاجرت‌ها، سرویس قیمت‌گذاری، منابع Filament و API v1.
   - اتصال اختیاری به `accounting_ir_products_services` و `accounting_ir_inventory_items`.
   - تست سرویس تبدیل قیمت با FX.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/commerce-catalog/*`
   - `app/Providers/Filament/AdminPanelProvider.php`
   - `app/Providers/Filament/TenantPanelProvider.php`
   - `composer.json`, `composer.lock`
   - `docs/commerce-catalog/*`

4) مهاجرت دیتابیس
   - `2025_12_30_000010_create_commerce_catalog_tables`
   - zero-downtime: بله (جداول جدید)
   - rollback: حذف جداول جدید

5) امنیت
   - endpoint جدید: API v1 کاتالوگ
   - rate limit: `commerce-catalog.api.rate_limit`
   - tenancy isolation: IAM + tenant scopes

6) i18n/RTL/شمسی
   - برچسب‌ها فارسی
   - تاریخ انتشار با `jalaliDate()`

7) تست‌ها
   - `php artisan test`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
