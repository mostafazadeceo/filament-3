[PR-004: هسته Feature Gates + اتصال به اشتراک‌ها]

1) هدف این PR (چرا)
   - پیاده‌سازی کنترل دسترسی مبتنی بر ویژگی‌ها با پشتیبانی از پلن‌ها و Overrideهای tenant.

2) چه چیزهایی تغییر کرد (What changed)
   - ایجاد پکیج `haida/feature-gates` با مدل‌ها، سرویس و مهاجرت‌ها.
   - اتصال FeatureGateService به AccessService برای enforcement سراسری.
   - مستندسازی جریان Feature Gate و ماتریس تست.
   - تست‌های پوشش‌دهنده override و plan features.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/feature-gates/*`
   - `packages/filamat-iam-suite/src/Services/AccessService.php`
   - `docs/05-workflows.mmd`
   - `docs/08-test-matrix.md`
   - `tests/Feature/FeatureGates/FeatureGateServiceTest.php`
   - `composer.json`, `composer.lock`

4) مهاجرت دیتابیس
   - `plan_features`
   - `tenant_feature_overrides`
   - zero-downtime: بله.
   - rollback: حذف جدول‌ها از migration down.

5) امنیت
   - endpoint جدید: ندارد.
   - rate limit: ندارد.
   - sanitization: ندارد.
   - tenancy isolation: overrides بر اساس tenant_id اعمال می‌شود.

6) i18n/RTL/شمسی
   - تغییری در UI ندارد (backend gate).

7) تست‌ها
   - `php artisan package:discover --ansi`
   - `php artisan test`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
