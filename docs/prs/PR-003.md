[PR-003: هسته رجیستری افزونه‌ها + چرخه عمر (install/enable/update/rollback)]

1) هدف این PR (چرا)
   - ایجاد رجیستری مرکزی افزونه‌ها، فعال/غیرفعال‌سازی برای هر tenant و ثبت نسخه‌های مهاجرت برای آینده‌نگری.

2) چه چیزهایی تغییر کرد (What changed)
   - ایجاد پکیج جدید `haida/platform-core` با سرویس لایف‌سایکل افزونه‌ها.
   - اضافه شدن جدول‌های رجیستری و مهاجرت افزونه‌ها.
   - تست‌های چرخه عمر افزونه‌ها.
   - مستند lifecycle در `/docs/07-plugin-lifecycle.md`.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/platform-core/*`
   - `composer.json`, `composer.lock`
   - `docs/07-plugin-lifecycle.md`
   - `tests/Feature/PlatformCore/PluginLifecycleManagerTest.php`

4) مهاجرت دیتابیس
   - ایجاد `plugin_registry`, `plugin_migrations`, `tenant_plugins`.
   - zero-downtime: بله (افزودنی و بدون تغییر رفتار قبلی).
   - rollback: حذف جدول‌ها از migration down.

5) امنیت
   - endpoint جدید: ندارد.
   - rate limit: ندارد.
   - sanitization: ندارد.
   - tenancy isolation: رکوردهای tenant_plugins به tenant_id محدود است.

6) i18n/RTL/شمسی
   - داده‌های manifest شامل `created_at_jalali` برای ثبت تاریخ جداسازی ماژول.

7) تست‌ها
   - `php artisan package:discover --ansi`
   - `php artisan test`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
