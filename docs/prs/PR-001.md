[PR-001: ممیزی اولیه و نقشه وابستگی‌ها (فقط مستندات)]

1) هدف این PR (چرا)
   - ایجاد دید دقیق از وضعیت ریپو، نسخه‌ها، ماژول‌ها، و وابستگی‌ها برای شروع Site OS بدون رگرسیون.

2) چه چیزهایی تغییر کرد (What changed)
   - تولید مستندات ممیزی و مفروضات.
   - ترسیم گراف وابستگی ماژول‌ها و افزودن بسته‌های جدید (platform-core/feature-gates/tenancy-domains).

3) فایل‌ها/ماژول‌های اصلی
   - `docs/00-assumptions.md`
   - `docs/01-audit.md`
   - `docs/02-dependency-graph.mmd`

4) مهاجرت دیتابیس
   - ندارد.
   - zero-downtime: بله (بدون تغییر دیتابیس).
   - rollback: حذف/بازگشت فایل‌های مستندات.

5) امنیت
   - endpoint جدید: ندارد.
   - rate limit: ندارد.
   - sanitization: ندارد.
   - tenancy isolation: تغییری ندارد.

6) i18n/RTL/شمسی
   - تغییر UI ندارد (مستندات فقط).

7) تست‌ها
   - `php artisan test`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت (تغییر UI ندارد)
