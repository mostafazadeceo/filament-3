PR-013: هسته Providerها + آداپتر Relograde

1) هدف این PR (چرا)
- ایجاد قراردادهای مشترک Providerها و رجیستری مرکزی برای اتصال ارائه‌دهندگان.
- اتصال Relograde به هسته جدید با آداپتر سازگار.

2) چه چیزهایی تغییر کرد (What changed)
- پکیج جدید `providers-core` با Registry، Job Dispatcher و ثبت لاگ اجرا.
- Job عمومی برای اجرای اکشن‌های Provider با ثبت نتیجه/خطا و رعایت tenancy.
- آداپتر `RelogradeProviderAdapter` که سرویس‌های موجود Relograde را به قرارداد جدید متصل می‌کند.
- مستندات SPEC/INSTALL/API برای Providers Core.
- تست پوشش اجرای Job و ثبت موفقیت.

3) فایل‌ها/ماژول‌های اصلی
- packages/providers-core/...
- packages/filament-relograde/src/Adapters/RelogradeProviderAdapter.php
- packages/filament-relograde/src/RelogradeServiceProvider.php
- docs/providers-core/{SPEC,INSTALL,API}.md

4) مهاجرت دیتابیس
- جدول جدید: `providers_core_job_logs`
- Zero-downtime: بله (جدول جدید)
- Rollback: حذف جدول

5) امنیت
- اجرای Jobها با tenant context کنترل می‌شود؛ در صورت نبود tenant، bypass فعال می‌شود.
- هیچ داده حساسی ذخیره نشده است؛ payload/result قابل کنترل از config.

6) i18n/RTL/شمسی
- UI جدیدی اضافه نشده؛ مستندات فارسی تکمیل شد.

7) تست‌ها
- php artisan test
- نتیجه: PASS

8) چک‌لیست PR
- [x] بدون Regression روی ماژول‌های موجود
- [x] تست‌ها PASS
- [x] docs به‌روز
- [x] استاندارد کدنویسی رعایت
- [x] امنیت بررسی
- [x] فارسی/RTL/شمسی رعایت
