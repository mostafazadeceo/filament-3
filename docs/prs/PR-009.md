[PR-009: افزودن CMS صفحات + وبلاگ (گیت‌شده)]

1) هدف این PR (چرا)
   - اضافه کردن صفحات ثابت، سئو، سایت‌مپ و وبلاگ عمومی برای Site OS با کنترل دسترسی و API.

2) چه چیزهایی تغییر کرد (What changed)
   - ایجاد پکیج‌های `haida/content-cms` و `haida/blog` با مدل‌ها، مهاجرت‌ها، سرویس‌ها، سیاست‌ها و منابع Filament.
   - افزودن روت‌های عمومی (صفحات، وبلاگ، sitemap.xml) با middleware `resolve.site`.
   - افزودن API v1 برای صفحات و وبلاگ + خروجی OpenAPI.
   - تست‌های واحد برای انتشار/پاکسازی محتوای CMS و وبلاگ.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/content-cms/*`
   - `packages/blog/*`
   - `app/Providers/Filament/AdminPanelProvider.php`
   - `app/Providers/Filament/TenantPanelProvider.php`
   - `composer.json`, `composer.lock`
   - `docs/content-cms/*`, `docs/blog/*`

4) مهاجرت دیتابیس
   - `2025_12_30_000008_create_content_cms_tables`
   - `2025_12_30_000009_create_blog_tables`
   - zero-downtime: بله (جداول جدید)
   - rollback: حذف جداول جدید

5) امنیت
   - endpoint جدید: API v1 + روت‌های عمومی صفحات/وبلاگ
   - rate limit: از config ماژول (`content-cms.api.rate_limit`, `blog.api.rate_limit`)
   - sanitization: صفحات با `PageBuilderService`، وبلاگ با `HtmlSanitizer`
   - tenancy isolation: scope و سیاست‌های IAM

6) i18n/RTL/شمسی
   - برچسب‌ها و UI فارسی
   - نمایش تاریخ با `jalaliDate()` در جدول‌ها

7) تست‌ها
   - `php artisan test`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
