[PR-021: TLS automation hooks + وضعیت صدور/تمدید + runbook]

1) هدف این PR (چرا)
   - افزودن مدل وضعیت TLS، hook صدور/تمدید، و runbook برای دامنه‌های سفارشی.

2) چه چیزهایی تغییر کرد (What changed)
   - افزودن ستون‌های TLS به `site_domains` و مدل وضعیت.
   - افزودن Certificate Manager/Provider و Job صدور گواهی.
   - افزودن فرمان تمدید گواهی‌ها.
   - بروزرسانی UI/API دامنه‌ها برای نمایش وضعیت TLS.
   - اضافه شدن مستند `docs/tls.md` و به‌روزرسانی workflow/test matrix.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/tenancy-domains/database/migrations/2025_12_30_000021_add_tls_fields_to_site_domains_table.php`
   - `packages/tenancy-domains/src/Tls/CertificateProvider.php`
   - `packages/tenancy-domains/src/Tls/CertificateResult.php`
   - `packages/tenancy-domains/src/Tls/NullCertificateProvider.php`
   - `packages/tenancy-domains/src/Services/CertificateManager.php`
   - `packages/tenancy-domains/src/Jobs/IssueCertificate.php`
   - `packages/tenancy-domains/src/Console/RenewCertificatesCommand.php`
   - `packages/tenancy-domains/src/Services/SiteDomainService.php`
   - `packages/tenancy-domains/src/Filament/Resources/SiteDomainResource.php`
   - `packages/tenancy-domains/routes/api.php`
   - `docs/tls.md`
   - `tests/Feature/TenancyDomains/SiteDomainTlsTest.php`

4) مهاجرت دیتابیس
   - افزودن ستون‌های TLS به `site_domains` (status/provider/mode/requested/issued/expires/error).
   - مسیر rollback: حذف ستون‌های TLS با مهاجرت down.

5) امنیت
   - صدور TLS فقط برای دامنه‌های تایید شده.
   - ثبت خطاها در `tls_error` برای audit.

6) i18n/RTL/شمسی
   - متن‌های UI فارسی؛ نمایش تاریخ‌ها در UI به شمسی.

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
