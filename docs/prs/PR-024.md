[PR-024: آداپتر HMAC درگاه tenant + تست‌ها]

1) هدف این PR (چرا)
   - افزودن آداپتور واقعی‌تر برای درگاه‌های tenant با ارتباط HTTP و امضای HMAC.

2) چه چیزهایی تغییر کرد (What changed)
   - افزودن `HmacGatewayAdapter` به payments orchestrator.
   - افزودن اتصال `connection_id` به meta و payload برای ساخت intent.
   - اضافه شدن تست برای ساخت intent با Http::fake.
   - به‌روزرسانی مستندات پرداخت.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/payments-orchestrator/src/Adapters/HmacGatewayAdapter.php`
   - `packages/payments-orchestrator/src/Services/PaymentIntentService.php`
   - `packages/payments-orchestrator/config/payments-orchestrator.php`
   - `tests/Feature/PaymentsOrchestrator/HmacGatewayAdapterTest.php`
   - `docs/payments-orchestrator/SPEC.md`

4) مهاجرت دیتابیس
   - ندارد.

5) امنیت
   - امضای HMAC برای درخواست ساخت intent و وبهوک‌ها.
   - استفاده از `webhook_secret` برای verify.

6) i18n/RTL/شمسی
   - بدون تغییر.

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
