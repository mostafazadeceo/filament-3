[PR-006: هسته سایت ساز (Site + انتشار) + پنل Filament]

1) هدف این PR (چرا)
   - ایجاد مدل های پایه سایت و چرخه انتشار برای شروع Site OS بدون ریسک رگرسیون.

2) چه چیزهایی تغییر کرد (What changed)
   - افزودن بسته `haida/site-builder-core` با مدل Site/Branding/PublishHistory.
   - سرویس انتشار (publish/preview/disable) و ثبت تاریخچه.
   - Resource فیلامنت برای مدیریت سایت ها و اکشن های انتشار.
   - اسناد SPEC/INSTALL/API برای ماژول.

3) فایل ها/ماژول های اصلی
   - `packages/site-builder-core/*`
   - `app/Providers/Filament/AdminPanelProvider.php`
   - `app/Providers/Filament/TenantPanelProvider.php`
   - `docs/site-builder-core/*`
   - `docs/08-test-matrix.md`
   - `tests/Feature/SiteBuilderCore/SitePublisherTest.php`

4) مهاجرت دیتابیس
   - `2025_12_30_000006_create_site_builder_core_tables`
   - zero-downtime: بله (جدول های جدید).
   - rollback: حذف جداول با migrate rollback.

5) امنیت
   - endpoint جدید: ندارد.
   - rate limit: ندارد.
   - sanitization: ندارد.
   - tenancy isolation: بله (BelongsToTenant + TenantContext).

6) i18n/RTL/شمسی
   - لیبل های فارسی برای فرم ها و جدول ها.
   - تاریخ انتشار با `jalaliDate()` نمایش داده می شود.

7) تست ها
   - `php artisan test`
   - نتیجه: PASS

8) چک لیست PR
   [x] بدون Regression روی ماژول های موجود
   [x] تست ها PASS
   [x] docs به روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
