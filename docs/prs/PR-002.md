[PR-002: زیرساخت بومی‌سازی/RTL/جلالی + نمایش زمان ایران]

1) هدف این PR (چرا)
   - فارسی‌محور کردن رابط، فعال‌سازی RTL، و استانداردسازی نمایش تاریخ/زمان (جلالی + منطقه زمانی ایران) بدون تغییر در ذخیره‌سازی UTC.

2) چه چیزهایی تغییر کرد (What changed)
   - تنظیم locale پیش‌فرض به `fa` و افزودن `display_timezone` برای نمایش زمان.
   - اعمال timezone نمایشی در `CalendarFormatter`.
   - اضافه شدن ترجمه‌های پایه فارسی برای کلیدهای عمومی.
   - افزودن تست‌های واحد برای جلالی و timezone نمایش.

3) فایل‌ها/ماژول‌های اصلی
   - `config/app.php`
   - `app/Support/Calendar/CalendarFormatter.php`
   - `lang/fa.json`
   - `tests/Unit/Support/Calendar/CalendarFormatterTest.php`
   - `docs/00-assumptions.md`

4) مهاجرت دیتابیس
   - ندارد.
   - zero-downtime: بله.
   - rollback: بازگردانی فایل‌های پیکربندی/کد و حذف فایل ترجمه/تست.

5) امنیت
   - endpoint جدید: ندارد.
   - rate limit: ندارد.
   - sanitization: ندارد.
   - tenancy isolation: تغییری ندارد.

6) i18n/RTL/شمسی
   - locale پیش‌فرض `fa` شد.
   - RTL بر اساس ترجمه‌های Filament فعال می‌شود.
   - نمایش تاریخ/زمان با جلالی و ارقام فارسی، و تبدیل به timezone ایران برای UI.

7) تست‌ها
   - `php artisan test`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
