[PR-027: سناریوهای deep-runner برای checkout/inventory + HMAC + TLS]

1) هدف این PR (چرا)
   - پوشش end-to-end در deep-runner برای جریان‌های کلیدی پرداخت، کاهش موجودی و درخواست TLS.

2) چه چیزهایی تغییر کرد (What changed)
   - اضافه شدن سناریوی checkout با کاهش موجودی و ثبت inventory docs.
   - اضافه شدن سناریوی ایجاد intent برای درگاه HMAC با Http fake.
   - اضافه شدن سناریوی درخواست TLS برای دامنه تأیید‌شده.

3) فایل‌ها/ماژول‌های اصلی
   - `scripts/deep_scenario_runner.php`

4) مهاجرت دیتابیس
   - ندارد.

5) امنیت
   - بدون تغییر.

6) i18n/RTL/شمسی
   - بدون تغییر.

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
