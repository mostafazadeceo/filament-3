[PR-023: ادغام Checkout با کاهش موجودی (Issue Docs)]

1) هدف این PR (چرا)
   - کاهش موجودی همزمان با پرداخت موفق و جلوگیری از فروش بیش‌ازحد.

2) چه چیزهایی تغییر کرد (What changed)
   - افزودن سرویس صدور سند انبار issue برای سفارش‌های پرداخت‌شده.
   - اتصال Checkout به سرویس موجودی پیش از پرداخت کیف پول.
   - افزودن پیکربندی inventory برای checkout.
   - به‌روزرسانی تست Checkout برای بررسی کاهش موجودی.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/commerce-checkout/src/Services/OrderInventoryService.php`
   - `packages/commerce-checkout/src/Services/CheckoutService.php`
   - `packages/commerce-checkout/src/CommerceCheckoutServiceProvider.php`
   - `packages/commerce-checkout/config/commerce-checkout.php`
   - `tests/Feature/CommerceCheckout/CheckoutFlowTest.php`

4) مهاجرت دیتابیس
   - ندارد.

5) امنیت
   - قفل کردن اقلام انبار برای جلوگیری از race condition.
   - کنترل منفی شدن موجودی مطابق تنظیمات حسابداری.

6) i18n/RTL/شمسی
   - بدون تغییر.

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
