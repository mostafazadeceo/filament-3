[PR-005: Tenancy Domains + Host Resolver (ساب‌دامین MVP)]

1) هدف این PR (چرا)
   - افزودن زیرساخت دامنه‌ها و middleware برای تطبیق Host به tenant/site.

2) چه چیزهایی تغییر کرد (What changed)
   - ایجاد پکیج `haida/tenancy-domains` شامل مدل، مهاجرت‌ها، و middleware.
   - افزودن SiteContext برای نگه‌داری host/tenant/site در درخواست‌ها.
   - سرویس ساده برای بررسی TXT/CNAME.
   - تست‌های resolver برای subdomain و دامنه تاییدشده.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/tenancy-domains/*`
   - `tests/Feature/TenancyDomains/ResolveTenantFromHostTest.php`
   - `composer.json`, `composer.lock`

4) مهاجرت دیتابیس
   - `site_domains`
   - zero-downtime: بله.
   - rollback: حذف جدول از migration down.

5) امنیت
   - Host allowlist + رد میزبان ناشناخته.
   - فقط دامنه‌های تاییدشده یا زیر دامنه root پذیرفته می‌شوند.

6) i18n/RTL/شمسی
   - تغییری در UI ندارد.

7) تست‌ها
   - `php artisan package:discover --ansi`
   - `php artisan test`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
