[PR-020: API/UI تأیید دامنه سفارشی + rate limit + وضعیت]

1) هدف این PR (چرا)
   - افزودن مسیرهای API/UI برای تایید دامنه سفارشی و ثبت وضعیت جهت تکمیل جریان دامنه‌ها.

2) چه چیزهایی تغییر کرد (What changed)
   - افزودن سرویس مدیریت درخواست/بررسی تایید دامنه و ثبت وضعیت.
   - ایجاد API برای ایجاد/ویرایش دامنه و اجرای تایید DNS.
   - اضافه شدن Resource فیلامنت برای مدیریت دامنه‌ها و اکشن‌های تایید.
   - مستندسازی جریان دامنه‌ها.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/tenancy-domains/src/Services/SiteDomainService.php`
   - `packages/tenancy-domains/src/Support/TenancyDomainsCapabilities.php`
   - `packages/tenancy-domains/src/Policies/SiteDomainPolicy.php`
   - `packages/tenancy-domains/src/TenancyDomainsPlugin.php`
   - `packages/tenancy-domains/src/Filament/Resources/SiteDomainResource.php`
   - `packages/tenancy-domains/src/Http/Controllers/Api/V1/DomainController.php`
   - `packages/tenancy-domains/src/Http/Resources/DomainResource.php`
   - `packages/tenancy-domains/routes/api.php`
   - `docs/domains.md`
   - `tests/Feature/TenancyDomains/SiteDomainVerificationTest.php`

4) مهاجرت دیتابیس
   - ندارد.

5) امنیت
   - اعمال rate limit برای endpointهای API.
   - کنترل دسترسی از طریق Policy و قابلیت‌ها.

6) i18n/RTL/شمسی
   - برچسب‌ها و متن‌های UI فارسی هستند.
   - نمایش تاریخ‌ها با فرمت جلالی در UI.

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
