[PR-016: رجیستری افزونه‌ها در UI + ثبت context مهاجرت‌ها]

1) هدف این PR (چرا)
   - مدیریت افزونه‌ها از طریق UI و ثبت دقیق context مهاجرت‌ها (کاربر/Correlation ID) برای ردیابی.

2) چه چیزهایی تغییر کرد (What changed)
   - افزودن مهاجرت برای ستون‌های `correlation_id` و `triggered_by_user_id` در `plugin_migrations`.
   - ثبت خودکار context مهاجرت‌ها از درخواست (Correlation ID) و کاربر جاری.
   - افزودن UI مدیریتی برای Plugin Registry و Tenant Plugins (Filament resources).
   - ثبت capabilityها و policyهای هسته پلتفرم.
   - اصلاح deep_scenario_runner برای بازسازی امن DB در SQLite.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/platform-core/src/PlatformCorePlugin.php`
   - `packages/platform-core/src/Filament/Resources/*`
   - `packages/platform-core/src/Services/PluginLifecycleManager.php`
   - `packages/platform-core/database/migrations/2025_12_30_000016_add_context_to_plugin_migrations.php`
   - `scripts/deep_scenario_runner.php`
   - `docs/07-plugin-lifecycle.md`
   - `docs/STATUS.md`, `docs/AUTOPILOT-STATE.md`

4) مهاجرت دیتابیس
   - `plugin_migrations`: افزودن `correlation_id` و `triggered_by_user_id`
   - zero-downtime: بله (ستون‌های جدید و nullable)
   - rollback: حذف ستون‌ها با migration down

5) امنیت
   - کنترل دسترسی UI با قابلیت‌های IAM (`platform.plugins.view/manage`)
   - ثبت Correlation ID و کاربر ثبت‌کننده در مهاجرت‌ها برای ردیابی

6) i18n/RTL/شمسی
   - UI فارسی در منابع Filament
   - تاریخ‌ها با `jalaliDateTime()`

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
