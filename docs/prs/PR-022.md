[PR-022: سخت‌سازی Host (TrustedHosts/TrustedProxies) + تست‌ها]

1) هدف این PR (چرا)
   - جلوگیری از Host header poisoning و محدود کردن دامنه‌های معتبر.

2) چه چیزهایی تغییر کرد (What changed)
   - فعال‌سازی TrustedHosts با allowlist پویا (root/allowed/custom verified domains).
   - افزودن پشتیبانی تنظیمات TrustedProxies با env.
   - افزودن تست برای پذیرش allowlist.

3) فایل‌ها/ماژول‌های اصلی
   - `bootstrap/app.php`
   - `tests/Feature/TenancyDomains/ResolveTenantFromHostTest.php`
   - `docs/08-test-matrix.md`
   - `docs/00-assumptions.md`

4) مهاجرت دیتابیس
   - ندارد.

5) امنیت
   - محدودسازی Hostهای معتبر در middleware TrustedHosts.
   - کنترل هدرهای Forwarded فقط در صورت تنظیم پروکسی‌های مورد اعتماد.

6) i18n/RTL/شمسی
   - بدون تغییر.

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
