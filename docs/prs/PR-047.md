[PR-047: جلوگیری از خطای نبود جدول site_domains در ResolveTenantFromHost]

1) هدف این PR (چرا)
   - جلوگیری از خطای تولید در صورت نبود جدول دامنه‌ها در پایگاه داده.

2) چه چیزهایی تغییر کرد (What changed)
   - افزودن بررسی `Schema::hasTable` و پذیرش سریع hostهای مجاز قبل از کوئری دامنه.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/tenancy-domains/src/Middleware/ResolveTenantFromHost.php`

4) مهاجرت دیتابیس
   - ندارد.

5) امنیت
   - کاهش سطح خطا و جلوگیری از crash روی hostهای مجاز.

6) i18n/RTL/شمسی
   - بدون تغییر.

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
