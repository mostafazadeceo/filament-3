[PR-008: صفحه ساز قالب محور + Sanitization]

1) هدف این PR (چرا)
   - ایجاد موتور Page Builder با ساختار JSON و انتشار امن با Sanitization.

2) چه چیزهایی تغییر کرد (What changed)
   - بسته `haida/page-builder` با مدل های Template و Revision.
   - سرویس Sanitizer و انتشار (publish/rollback) برای محتوا.
   - Resource فیلامنت برای مدیریت قالب ها.
   - مستندات و تست های سرویس.

3) فایل ها/ماژول های اصلی
   - `packages/page-builder/*`
   - `app/Providers/Filament/AdminPanelProvider.php`
   - `app/Providers/Filament/TenantPanelProvider.php`
   - `docs/page-builder/*`
   - `tests/Feature/PageBuilder/PageBuilderServiceTest.php`
   - `docs/08-test-matrix.md`

4) مهاجرت دیتابیس
   - `2025_12_30_000007_create_page_builder_tables`
   - zero-downtime: بله (جدول های جدید).
   - rollback: حذف جداول با migrate rollback.

5) امنیت
   - sanitization: فعال روی کلیدهای حساس (html/rich_text/content/body/description).
   - tenancy isolation: بله (BelongsToTenant + TenantContext).

6) i18n/RTL/شمسی
   - برچسب های فارسی در پنل.

7) تست ها
   - `php artisan test`
   - نتیجه: PASS

8) چک لیست PR
   [x] بدون Regression روی ماژول های موجود
   [x] تست ها PASS
   [x] docs به روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
