[PR-025: UI لاگ‌های Provider + بازپردازش dead-letter]

1) هدف این PR (چرا)
   - فراهم کردن مشاهده و بازپردازش jobهای ناموفق Provider برای پایداری عملیاتی.

2) چه چیزهایی تغییر کرد (What changed)
   - افزودن Resource فیلامنت برای لاگ‌های Provider.
   - افزودن سرویس بازپردازش و اکشن Reprocess.
   - ثبت قابلیت‌ها و Policy برای مجوزها.
   - افزودن تست بازپردازش و بروزرسانی test matrix.

3) فایل‌ها/ماژول‌های اصلی
   - `packages/providers-core/src/Filament/Resources/ProviderJobLogResource.php`
   - `packages/providers-core/src/Filament/Resources/ProviderJobLogResource/Pages/ListProviderJobLogs.php`
   - `packages/providers-core/src/Filament/Resources/ProviderJobLogResource/Pages/ViewProviderJobLog.php`
   - `packages/providers-core/src/Services/ProviderJobReprocessService.php`
   - `packages/providers-core/src/Policies/ProviderJobLogPolicy.php`
   - `packages/providers-core/src/Support/ProvidersCoreCapabilities.php`
   - `packages/providers-core/src/ProvidersCorePlugin.php`
   - `tests/Feature/ProvidersCore/ProviderJobReprocessTest.php`

4) مهاجرت دیتابیس
   - ندارد.

5) امنیت
   - دسترسی Reprocess بر اساس مجوز `provider.job_log.manage` کنترل می‌شود.

6) i18n/RTL/شمسی
   - برچسب‌ها فارسی و تاریخ‌ها جلالی نمایش داده می‌شود.

7) تست‌ها
   - `php artisan test`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_demo.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync ./scripts/demo-e2e.sh`
   - `DB_CONNECTION=sqlite DB_DATABASE=/tmp/haida_deep.sqlite CACHE_STORE=array QUEUE_CONNECTION=sync php scripts/deep_scenario_runner.php`
   - نتیجه: PASS

8) چک‌لیست PR
   [x] بدون Regression روی ماژول‌های موجود
   [x] تست‌ها PASS
   [x] docs به‌روز
   [x] استاندارد کدنویسی رعایت
   [x] امنیت بررسی
   [x] فارسی/RTL/شمسی رعایت
